"use client";
import { useEffect, useRef } from "react";
export function ParticleField(){const ref=useRef<HTMLCanvasElement>(null);useEffect(()=>{const canvas=ref.current;if(!canvas)return;const ctx=canvas.getContext("2d");if(!ctx)return;let raf=0,w=0,h=0;const dots=Array.from({length:70},()=>({x:Math.random(),y:Math.random(),r:Math.random()*1.4+.3,v:Math.random()*.0003+.0001}));const resize=()=>{w=canvas.width=innerWidth*devicePixelRatio;h=canvas.height=innerHeight*devicePixelRatio;ctx.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0)};const draw=()=>{ctx.clearRect(0,0,w,h);dots.forEach(d=>{d.y-=d.v;if(d.y<0)d.y=1;ctx.fillStyle=`rgba(${d.r>1?"105,220,255":"143,91,255"},${.24+d.r*.18})`;ctx.beginPath();ctx.arc(d.x*innerWidth,d.y*innerHeight,d.r,0,Math.PI*2);ctx.fill()});raf=requestAnimationFrame(draw)};resize();addEventListener("resize",resize);draw();return()=>{cancelAnimationFrame(raf);removeEventListener("resize",resize)}},[]);return <canvas ref={ref} className="particles" aria-hidden="true"/>}
